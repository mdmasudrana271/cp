#include <bits/stdc++.h>
#define ll long long int
using namespace std;

int main()
{
    // write c++ program code
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    string s;
    cin >> s;
    int nb, ns, nc, pb, ps, pc;
    cin >> nb >> ns >> nc;
    cin >> pb >> ps >> pc;
    ll r;
    cin >> r;
    auto ok = [&](ll mid) {

    };

    return 0;
}